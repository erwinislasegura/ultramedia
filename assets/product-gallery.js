(()=>{document.querySelectorAll('[data-product-slider]').forEach((slider,index)=>{const images=[...slider.querySelectorAll('img')];if(images.length<2)return;let current=0;setInterval(()=>{images[current].classList.remove('active');current=(current+1)%images.length;images[current].classList.add('active')},2800+(index%3)*350)});const main=document.getElementById('detailMainImage');document.querySelectorAll('.detail-thumb').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.detail-thumb').forEach(x=>x.classList.remove('active'));button.classList.add('active');if(main)main.src=button.dataset.image}));const checks=[...document.querySelectorAll('.photo-selector input[type="checkbox"]')],count=document.getElementById('selectedCount'),total=document.getElementById('selectedTotal'),submit=document.getElementById('addSelected');function money(value){return '$'+new Intl.NumberFormat('es-CL').format(value)}function update(){const selected=checks.filter(x=>x.checked);if(count)count.textContent=selected.length;if(total)total.textContent=money(selected.reduce((sum,x)=>sum+Number(x.dataset.price||0),0));if(submit)submit.disabled=selected.length===0}checks.forEach(check=>check.addEventListener('change',update));update()})();
