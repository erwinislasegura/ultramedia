const form=document.querySelector('.photo-upload-form'),isEdit=form?.dataset.edit==='true',input=document.querySelector('#files'),drop=document.querySelector('#drop'),queue=document.querySelector('#queue'),fileCount=document.querySelector('#fileCount'),publish=document.querySelector('#publish'),ready=document.querySelector('#ready'),removeBtn=document.querySelector('#remove'),allBox=document.querySelector('#all');let items=[];const fmt=n=>(n/1048576).toFixed(1)+' MB';
function sync(){if(!input||isEdit)return;const dt=new DataTransfer();items.filter(x=>x.on).forEach(x=>dt.items.add(x.file));input.files=dt.files}
function render(){if(isEdit){const selected=items[0];if(selected){queue.innerHTML=`<article><img src="${selected.url}"><span><b>${selected.file.name}</b><small>${fmt(selected.file.size)} · reemplazará el archivo actual</small></span><em>NUEVA</em></article>`;fileCount.textContent='1 archivo nuevo';ready.textContent='Nueva fotografía lista para guardar';publish.disabled=false}else{fileCount.textContent='ARCHIVO ACTUAL';ready.textContent='Fotografía lista para actualizar';publish.disabled=false}return}if(!queue)return;queue.innerHTML=items.length?'':'<p>NO HAY ARCHIVOS EN LA COLA</p>';items.forEach((x,i)=>queue.insertAdjacentHTML('beforeend',`<article><input type="checkbox" data-i="${i}" ${x.on?'checked':''}><img src="${x.url}"><span><b>${x.file.name}</b><small>${fmt(x.file.size)}</small></span><em>LISTA</em></article>`));fileCount.textContent=items.filter(x=>x.on).length+' archivos';publish.disabled=!items.some(x=>x.on);ready.textContent=items.some(x=>x.on)?items.filter(x=>x.on).length+' fotografías listas':'Lote sin archivos';queue.querySelectorAll('input').forEach(c=>c.onchange=()=>{items[c.dataset.i].on=c.checked;sync();render()});sync()}
function add(files){const valid=[...files].filter(f=>f.type.startsWith('image/'));if(isEdit){items.forEach(x=>URL.revokeObjectURL(x.url));items=valid.slice(0,1).map(file=>({file,url:URL.createObjectURL(file),on:true}));if(items[0]){const dt=new DataTransfer();dt.items.add(items[0].file);input.files=dt.files}}else valid.forEach(file=>{const key=[file.name,file.size,file.lastModified].join(':');if(!items.some(x=>x.key===key))items.push({file,key,url:URL.createObjectURL(file),on:true})});render()}
input?.addEventListener('change',e=>{if(e.target.files.length)add(e.target.files)});['dragover','dragenter'].forEach(name=>drop?.addEventListener(name,e=>{e.preventDefault();drop.classList.add('drag')}));drop?.addEventListener('dragleave',()=>drop.classList.remove('drag'));drop?.addEventListener('drop',e=>{e.preventDefault();drop.classList.remove('drag');add(e.dataTransfer.files)});removeBtn?.addEventListener('click',()=>{if(isEdit){items.forEach(x=>URL.revokeObjectURL(x.url));items=[];if(input)input.value='';location.reload();return}const removed=items.filter(x=>x.on),remaining=items.filter(x=>!x.on);removed.forEach(x=>URL.revokeObjectURL(x.url));items=remaining;if(!items.length&&input)input.value='';if(allBox)allBox.checked=items.length>0&&items.every(x=>x.on);render()});allBox?.addEventListener('change',()=>{items.forEach(x=>x.on=allBox.checked);render()});
const wmInput=document.querySelector('#wmInput'),wmText=document.querySelector('#wmText'),wmToggle=document.querySelector('#wmToggle'),wmImageInput=document.querySelector('#wmImageInput');wmInput?.addEventListener('input',e=>wmText.textContent=e.target.value);wmToggle?.addEventListener('change',e=>wmText.hidden=!e.target.checked);wmImageInput?.addEventListener('change',e=>{const file=e.target.files[0];if(file){wmText.textContent='IMAGEN SELECCIONADA';wmText.style.fontSize='14px'}});function watermarkFields(){const image=document.querySelector('[name="watermark_type"][value="image"]')?.checked;if(wmImageInput?.closest('label'))wmImageInput.closest('label').hidden=!image;if(wmInput?.closest('label'))wmInput.closest('label').hidden=!!image}document.querySelectorAll('[name="watermark_type"]').forEach(r=>r.addEventListener('change',watermarkFields));watermarkFields();render();
